# Doubly Linked List in Rust (fine-tuned)
This implementation was inspired by curiosity towards data structures and algorithms. It is standard and has a little fine-tuning to track free memory after `alloc`/`dealloc`

## Usage
```
cargo run -r
```
